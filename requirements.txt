# 运行依赖（Python 3.10–3.12, Windows x64）
# 安装：venv\Scripts\pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# 音频
sounddevice==0.5.6
soundfile==0.14.0

# 语音识别（含官方模型下载器）
moonshine-voice==0.1.5

# 翻译
ctranslate2==4.8.2
sentencepiece==0.2.2

# 模型推理
onnxruntime==1.30.0
numpy>=2.0

# 模型下载（moonshine 官方下载器的依赖会被自动带上，这里显式锁定常用项）
requests>=2.31
tqdm>=4.66

# 界面快捷键
keyboard==0.13.5
